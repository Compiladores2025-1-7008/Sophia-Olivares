package main.java;

public enum ClaseLexica {
    INT, FLOAT, IF, ELSE, WHILE, // Palabras clave
    NUMERO, ID,                  // Números e identificadores
    PYC, COMA, LPAR, RPAR        // Símbolos de puntuación
}
